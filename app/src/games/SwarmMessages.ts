// What is this? See SwarmMessenger or Swarm.

/*
import { SignalData } from 'simple-peer';

import { SocketMessage } from '../models/SocketMessage';

export type AddPeerMessage = SocketMessage<'AddPeerMessage', { userId: string }>;
export type SignalMessage = SocketMessage<
	'SignalMessage',
	{ to: string; from: string; signal: string | SignalData }
>;
export type SwarmMessage = AddPeerMessage | SignalMessage;
*/
